use_frameworks!

target "DailyUpdate" do
    pod 'Alamofire', '~> 3.4'
end

plugin 'cocoapods-keys', {
          :project => "Eidolon",
          :keys => [
               		"ProjectIdKey",
                    "UserIdKey",
                    "TicketIdKey",
                    ]
}
